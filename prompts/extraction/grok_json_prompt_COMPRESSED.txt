JSON OUTPUT FORMAT (ExtractionResult Pydantic schema enforcement):

CRITICAL FIELD NAMES (exact matches required):
entity_name (NOT name), entity_type (NOT type), description (NOT desc), source_text (optional), relationships (array)

CRITICAL RELATIONSHIP FORMAT:
source_entity and target_entity MUST be FULL ENTITY OBJECTS with {entity_name,entity_type,description}, NOT string references
WRONG: {"source_entity":"Factor 1",...}
CORRECT: {"source_entity":{"entity_name":"Factor 1","entity_type":"evaluation_factor","description":"..."},...}

OUTPUT STRUCTURE:
{"entities":[{entity objects with type-specific fields}],"relationships":[{source_entity:{},target_entity:{},relationship_type:"",description:""}]}

Do NOT include: markdown formatting (```json), preamble text, explanations
Output: Raw JSON string matching ExtractionResult schema only
