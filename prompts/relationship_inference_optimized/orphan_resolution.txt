Orphan Resolution (Compact)

Goal:
- Minimize isolated entities by inferring safe links.

Rules:
- Use proximity, shared identifiers (e.g., CDRL IDs), hierarchy context to attach orphans.
- If uncertain, annotate candidate links for review rather than forcing.

Output:
- Entities: any.
- Relationships: inferred links with confidence notes.
