# Phase 3 / Phase 4 archive

Historical phase write-ups kept for audit trail. These were in-flight
planning + design notes for the Skills + MCP build (Phases 3.x and 4.x,
roughly issues #119–#136).

The work landed in `main` via the merge of
`120-skills-spec-compliance` (tag `v1.2.0`). Current status:

- **Skills** — see each skill's `SKILL.md` under `.github/skills/<name>/`
- **MCP vendoring** — see each MCP's `UPSTREAM.md` under `tools/mcps/<name>/`
- **Skill taxonomy / spec compliance** — see [`../../SKILL_TAXONOMY.md`](../../SKILL_TAXONOMY.md)
  and [`../../SKILL_SPEC_COMPLIANCE.md`](../../SKILL_SPEC_COMPLIANCE.md)
- **Architecture overview** — see [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md)

Nothing in this folder is loaded by code or tests; it is reference-only.
