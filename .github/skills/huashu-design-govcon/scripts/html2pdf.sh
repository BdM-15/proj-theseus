#!/usr/bin/env bash
# html2pdf.sh — print-fidelity PDF for compliance matrices and one-pagers.
# Defaults: Letter, 0.75" margins, embed fonts, no headers/footers.
# Usage: ./html2pdf.sh build/matrix.html dist/matrix.pdf
#
# STATUS: skeleton — implement with playwright or wkhtmltopdf in a follow-up.
echo "html2pdf.sh skeleton — implementation TODO. See SKILL.md." >&2
exit 2
