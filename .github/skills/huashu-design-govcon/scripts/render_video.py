#!/usr/bin/env python3
"""render_video.py — narrate a slide deck with TTS and stitch to MP4.

Defaults: ≤90 s, 1920×1080, 30 fps, slate-on-bone palette, voiceover from
the slide notes field. Reads build/context.json for entity payloads.

STATUS: skeleton. Wire to pyttsx3 / coqui + ffmpeg in a follow-up commit.
"""
import sys
print("render_video.py skeleton — implementation TODO. See SKILL.md.", file=sys.stderr)
sys.exit(2)
