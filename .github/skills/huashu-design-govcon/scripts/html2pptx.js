#!/usr/bin/env node
/**
 * html2pptx.js — convert an HTML deck into editable .pptx.
 *
 * Adapted from alchaincyf/huashu-design with GovCon defaults:
 *   - 16:9 1920x1080 slide size
 *   - Source Serif 4 / Source Sans 3 font embedding
 *   - Slate-on-bone palette (no neon)
 *   - Reads entity payload from build/context.json
 *
 * Usage:  node html2pptx.js --in build/deck.html --out dist/deck.pptx
 *
 * STATUS: skeleton — wire to pptxgenjs + jsdom in a follow-up commit.
 */
console.error("html2pptx.js skeleton — implementation TODO. See SKILL.md.");
process.exit(2);
