{
  "name": "huashu-design-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Node-side dependencies for huashu-design renderer scripts (html2pptx, export_deck_pdf, render-video). Vendored alongside the skill so Theseus's tools-mode runtime can invoke them via run_script. NOT a published package.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "playwright": "^1.50.0",
    "pptxgenjs": "^3.12.0",
    "sharp": "^0.33.0"
  }
}
